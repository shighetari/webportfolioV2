# Chatbot Enhancement Todos

## ✅ Completed (Session 2)
- [x] **Move resize handle to bottom-left** (both Assistant.tsx and ChatBox.tsx)
  - Applied CSS `direction: rtl` trick to flip resize handle
  - Reset `direction: ltr` on all content to maintain normal text flow

- [x] **Add full markdown support** with react-markdown
  - Integrated ReactMarkdown in both chatbot components
  - Comprehensive styling: code blocks, inline code, links, lists, headings, blockquotes, tables
  - Dark mode support for all markdown elements

- [x] **Add connection status indicator**
  - Real-time status badge (Online/Offline/Sending/Receiving)
  - Color-coded: Green (connected), Yellow (active), Red (disconnected)
  - Pulsing animation during activity
  - Full dark mode support

- [x] **Improve error handling**
  - Categorized error types: Network, Auth, Rate Limit, Server, Timeout
  - Specific error messages for each category
  - Actionable suggestions (e.g., "Wait a minute and retry")
  - Enhanced error UI with title, details, and suggestions

## ✅ Completed (Session 1)
- [x] Update CLAUDE.md with dev server instructions
- [x] Add API key validation to server/dev-server.js
- [x] Add API key validation to api/chat.ts
- [x] Modernize all SASS files (zero warnings)
- [x] Install react-markdown dependency
- [x] Test chatbot functionality

## 📌 Important Notes
- **Always use `npm run dev:full`** for chatbot development
- Using `npm run dev` alone will cause ECONNREFUSED errors
- All 4 icons preserved: Fox (mascot), Tux (assistant), Kali (user), Arch (input)
- AI Gateway configured and working with Groq Llama 3.3 70B
- Resize handle now on bottom-left (desktop only, disabled on mobile)
- Full markdown rendering in chat messages
